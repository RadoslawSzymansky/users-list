import styled from 'styled-components';

const UsersListWrapper = styled.ul`
`;

const Li = styled.li`
  margin-left: 0;
  list-style: none;

  > span {
    padding-left: 15px;
    color: #aaa;
  }
`;

export { UsersListWrapper, Li };
